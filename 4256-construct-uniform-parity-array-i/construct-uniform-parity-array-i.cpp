class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        int n=nums1.size();
        if(n==1) return true;
        // int o=0,e=0;
        // for(int i=0;i<n;i++){
        //     if(nums1[i]&1)o++;
        //     else e++;
        // }
        // if(o==0 || e==0) return true;
        // if(o>0) return true;
        return true;
    }
};